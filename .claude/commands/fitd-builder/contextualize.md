---
description: FITD Builder - contextualize an existing menu offer to a niche (hook + story + hydrated OfferIQ pillars + ad/VSL/landing copy), delivered as a markdown package plus a visual HTML artifact
argument-hint: "[niche + which menu offer, e.g. 'single-location med spas + Speed-to-Lead']"
---

Invoke the **fitd-builder** skill and run its `contextualize` workflow
(`tasks/contextualize-offer.md`): resolve the live offer menu, select the chosen offer, research the
niche, write the one specific problem, craft the Expert Secrets narrative, hydrate all seven OfferIQ
pillars for the niche, and produce the deployable assets, then write the package to one markdown
file and render it as a visual, self-contained HTML artifact (a native Artifact, or a `.html` file
when Artifact is unavailable). Context: $ARGUMENTS
